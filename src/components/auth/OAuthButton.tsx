import Button from '@/components/common/Button';

export default function OAuthButton() {
  return (
    <Button className="w-full bg-gray-200 dark:bg-gray-600 text-gray-900 dark:text-white hover:bg-gray-300 dark:hover:bg-gray-500">
      Sign in with Google
    </Button>
  );
}